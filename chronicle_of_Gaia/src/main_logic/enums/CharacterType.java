package main_logic.enums;

/**
 * Legacy placeholder kept only to avoid stale file references.
 *
 * <p>Player classes are now loaded from the database through the class catalog,
 * so this type is no longer part of the character creation or persistence flow.</p>
 */
@Deprecated
public final class CharacterType {
    private CharacterType() {
    }
}
